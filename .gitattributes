# Normalize line endings: store LF in the repository and check out LF everywhere.
* text=auto eol=lf

# Go tooling expects LF; gofmt writes LF.
*.go text eol=lf
go.mod text eol=lf
go.sum text eol=lf

# Windows scripts need CRLF to run.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
